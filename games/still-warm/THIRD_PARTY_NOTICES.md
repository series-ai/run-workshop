# Third-party notices

The app uses React, Three.js, React Three Fiber, Drei, and React Three
Postprocessing. Their packages include their license notices. The
`postprocessing` package uses the Zlib license.

The RUN game SDK and RUN agent SDK include their own license files.
See the installed packages for their terms. The shared `dither-kit` code comes
from this repository. No sample images, music, or video from the dither demo
are included.

The creature was generated with RUN tools. See [ASSETS.md](ASSETS.md).
The remaining room geometry and sound are created by this app's source code.
