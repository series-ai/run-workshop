{
  "name": "dither-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Engine-agnostic GPU dithering core: config → uniforms → GLSL fragment shader, with retro palettes and named presets. Shipped as TypeScript source.",
  "type": "module",
  "license": "SEE LICENSE IN ../../LICENSE.md",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^4.1.10"
  }
}
